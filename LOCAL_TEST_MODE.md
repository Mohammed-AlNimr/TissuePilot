# Local test mode

Run the API locally and use the deterministic physics endpoints without external AI providers. External literature providers are optional and should be used as evidence discovery, not as automatic truth.
