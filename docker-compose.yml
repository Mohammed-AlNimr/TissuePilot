services:
  api:
    build: ./backend
    ports:
      - "8000:8000"
    env_file:
      - ./backend/.env.example
  web:
    build: ./frontend
    ports:
      - "8080:80"
    depends_on:
      - api
